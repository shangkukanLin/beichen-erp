# 2026-08-10 工作日志

## 前端模块审查（beichen-erp-web）
按风险优先级逐模块审查前端代码与后端契约一致性，流程：出方案→用户【同意】→落地→记记忆。

已整改模块：
1. supplier：移除冗余删除按钮（handleDelete + deleteSupplier import），后端实际是停用端点，功能重复。
2. inventory/brand：loadData catch 加 ElMessage.error 提示。
3. finance/receipt.vue：移除编辑按钮（handleEdit），后端无修改接口，编辑=新建会致应收重复核销/余额重复更新（资金事故）。
4. outsource/order/delivery.vue：删调试 console.log、加载失败加 ElMessage.error。
5. purchase/+sale/（2026-08-10 落地，3 个 P2）：
   - sale/return/add.vue buildPayload 精简：移除冗余脏字段 id/customerName/productName。
   - sale/return/add.vue submit 加 catch + ElMessage.error 失败提示。
   - purchase/order/add.vue items 去掉恒为 undefined 的 materialCode。
   核对通过项：路径全部一致；销售退货 delete 置 CANCELLED（仅草稿）；采购退货无删除按钮；余额/库存由后端审核原子更新前端不传。

待审模块（P2）：dev/、material/、dashboard/、system 其余、error/、api/common.ts。

6. dev/（2026-08-10 落地，3 个 P2 健壮性）：
   - bom/index.vue：loadProjects 加 ElMessage.error 失败提示（原静默）+ 引入 ElMessage。
   - drawing/index.vue：同上处理。
   - project/edit.vue：loadDevMaterials、loadRelatedOrders 的 console.warn 升级为 ElMessage.error；loadBugs 补 catch + ElMessage.error（原无 catch 会抛到 onMounted 未捕获）。
   核对通过项：路径全部一致（/api/dev/project/*、/bom-type/page、/phase-template/list、/purchase-item/page、/purchase-item/project/{id}）；Project.status 存中文阶段名直接展示正确；DevPurchaseItem 字段映射一致；删除语义与后端一致；未关联项目选项逻辑正确；PageParam.pageNum 默认1，仅传 pageSize 正常。
   N+1 性能隐患（bom/drawing 概览每项目串行请求）仅作建议，未改（后端无批量接口）。

7. material/（2026-08-10 落地，1 个 P1 + 1 个 P2）：
   - 后端事实：ProductController.delete 实为 service.discontinue(id) 即「停用」(status=DISCONTINUED)，非物理删除。
   - 问题1(P1)：src/views/material/index.vue handleDelete 调用 /product/${id}/check-delete 关联校验，但后端 ProductController 无此端点（必404被静默吞）→ 防护失效；且"删除"实为停用，前端"删除成功"措辞误导。修复：删 check-delete 死调用块、确认框文案改"停用"并提示可在停售Tab查看、成功提示改"已停用"、移除物理删除翻页回退逻辑。
   - 问题2(P2)：loadBrands 失败 console.warn 升级为 ElMessage.error。
   核对通过项：product 路径一致；/brand/enabled 存在返回 List<Brand>；Product 字段映射一致；低库存预警前端计算无误传；新增/编辑有 catch。

8. dashboard/（2026-08-10 落地，1 个 P1 + 1 个 P2）：
   - 问题1(P1 真实Bug)：src/views/dashboard/index.vue 第332行 request.post('/dev/project/timelines/batch', {projectIds:[...]}) 传对象，但后端 ProjectController.batchTimelines 签名 @RequestBody List<Long> 期望裸数组 → Spring 反序列化失败(400)被外层catch静默吞，进行中项目"当前阶段/计划完成/进度"三列永远'-'。修复：改为传裸数组 activeProjects.map(p=>p.id)。
   - 同一传参bug也存在 src/views/dev/project/index.vue 第65行（未改，已标注待修）。

9. dev/project/index.vue batch 传参 bug 修复（2026-08-10 落地）：
   - 第65行 request.post('/dev/project/timelines/batch', {projectIds: ids}) 改为 request.post(..., ids) 传裸数组，匹配后端 @RequestBody List<Long>。修复后研发项目页阶段/进度正常显示。lint 0 错误。
   - 问题2(P2)：第358行物料订单 m.status==='已确认' 死分支（后端 MaterialOrderStatus 无此状态，确认后直接转收货中），改为只判 '待确认'。数值不变，清理误导代码。
   核对通过项：各 /page 端点路径与 total/records 一致；委外加工单 status(待确认/生产中/已完成/已取消)一致；ProjectStatus 枚举一致；ProjectTimeline.status/statusName/plannedEnd 字段一致；权限 hasModule/hasMenu 基于 menus.routeName 正确。
   lint：改动行无新错误；报 vue/vue-router 模块找不到为环境层 pre-existing（npm 类型解析），非本次引入。
